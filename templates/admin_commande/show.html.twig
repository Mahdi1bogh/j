{% extends 'base_molka.html.twig' %}

{% block title %}Commande{% endblock %}

{% block tableau %}
    <h1>Détails Commande</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdCommande</th>
                <td>{{ commande.idCommande }}</td>
            </tr>
            <tr>
                <th>TotalPoints</th>
                <td>{{ commande.totalPoints }}</td>
            </tr>
            <tr>
                <th>IdUser</th>
                <td>{{ commande.idUser }}</td>
            </tr>
            <tr>
                <th>DateCommande</th>
                <td>{{ commande.dateCommande ? commande.dateCommande|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ commande.adresse }}</td>
            </tr>
            <tr>
                <th>Etat</th>
                <td>{{ commande.etat }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_commande_index') }}" class="btn btn-secondary">back to list</a>

    <a href="{{ path('app_admin_commande_edit', {'idCommande': commande.idCommande}) }}" class="btn btn-outline-warning">edit</a>
<button class="btn btn-danger">
    {{ include('admin_commande/_delete_form.html.twig') }}
</button>
{% endblock %}
