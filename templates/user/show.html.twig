{% extends 'base.html.twig' %}

{% block title %}User{% endblock %}


    {% block tableau %}
        <div class="page-wrapper">
            <div class="page-breadcrumb bg-white">
                <div class="row align-items-center">

                    <div class="col-lg-12 col-sm-8 col-md-8 col-xs-12">
                        <div class="d-md-flex">
                            <ol class="breadcrumb ms-auto">
                                <li><a href="#" class="fw-normal">Dashboard</a></li>
                            </ol>


                        </div>
                    </div>
                    <div class="col-lg-11 col-sm-4 col-md-4 col-xs-9">
                        <div class="d-md-flex">
                            <ol class="breadcrumb ms-auto">
                                <a class="get-started-btn scrollto" href=" {{path('home') }}">Page membre</a>
                            </ol>


                        </div>
                    </div>
                </div>
            </div>
            <div class="container-fluid">
                <div class="row">
                    <div class="col-sm-12">
                        <div class="white-box">


                            <table class="table-responsive">


                                <table class="table">
                                    <tbody>

                                    <tr>
                                        <th>Nom_user</th>
                                        <td>{{ user.nomUser }}</td>
                                    </tr>
                                    <tr>
                                        <th>Prenom_user</th>
                                        <td>{{ user.prenomUser }}</td>
                                    </tr>
                                    <tr>
                                        <th>Tel_user</th>
                                        <td>{{ user.telUser }}</td>
                                    </tr>
                                    <tr>
                                        <th>Email_user</th>
                                        <td>{{ user.emailUser }}</td>
                                    </tr>
                                    </tbody>
                                </table>
                                <table class="table">
                                    <th>
                                        <div class="button"  style="width:90px;height:60px;text-align:right; margin: 20px;">
                                            <a href="{{ path('app_user_index') }}"
                                               class="btn d-grid btn-danger text-white" target="_self">
                                                back to list</a>
                                        </div>
                                    </th>
                                    <td>
                                        <div class="button"  style="width:90px;height:60px;text-align:right; margin: 20px;">
                                            <a href="{{ path('app_user_edit', {'id_user': user.idUser}) }}"
                                               class="btn d-grid btn-danger text-white" target="_self">
                                                edit</a>
                                            {{ include('user/_delete_form.html.twig') }}
                                        </div>
                                    </td>

                                </table>
                            </table>
                        </div>
                    </div>
                </div>
            </div>

        </div>
        <footer class="footer text-center">
        </footer>
        </div>
        </div>
    {% endblock %}
