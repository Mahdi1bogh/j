<form method="post" action="{{ path('app_match1_delete', {'id_match1': match1.idMatch1}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ match1.idMatch1) }}">
    <button class="btn d-grid btn-danger text-white">Delete</button>
</form>
