{% extends 'base.html.twig' %}

{% block title %}Match{% endblock %}

{% block tableau %}
<div class="page-wrapper">
    <!-- ============================================================== -->
    <!-- Bread crumb and right sidebar toggle -->
    <!-- ============================================================== -->
    <div class="page-breadcrumb bg-white">
        <div class="row align-items-center">
            <div class="col-lg-3 col-md-4 col-sm-4 col-xs-12">
                <h4 class="page-title">Users list</h4>
            </div>
            <div class="col-lg-9 col-sm-8 col-md-8 col-xs-12">
                <div class="d-md-flex">
                    <ol class="breadcrumb ms-auto">



                    </ol>

                </div>
            </div>
        </div>
        <!-- /.col-lg-12 -->
    </div>
    <!-- ============================================================== -->
    <!-- End Bread crumb and right sidebar toggle -->
    <!-- ============================================================== -->
    <!-- ============================================================== -->
    <!-- Container fluid  -->
    <!-- ============================================================== -->
    <div class="container-fluid">
        <!-- ============================================================== -->
        <!-- Start Page Content -->
        <!-- ============================================================== -->
        <div class="row">
            <div class="col-sm-12">
                <div class="white-box">
                    <h3 class="box-title">Users list</h3>

                    <div class="table-responsive">
    <h1>Match1 </h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id_match1</th>
                <th>Nom_equipe1</th>
                <th>Nom_equipe2</th>
                <th>Date_match1</th>
                <th>Resultat_match1</th>
                <th>Lieu_match1</th>
                <th>Email_lieu</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for match1 in matchs %}
            <tr>
                <td>{{ match1.idMatch1 }}</td>
                <td>{{ match1.nomEquipe1 }}</td>
                <td>{{ match1.nomEquipe2 }}</td>
                <td>{{ match1.dateMatch1 }}</td>
                <td>{{ match1.resultatMatch1 }}</td>
                <td>{{ match1.lieuMatch1 }}</td>
                <td>{{ match1.EmailLieu }}</td>
                <td>
                    <a href="{{ path('app_match1_show', {'id_match1': match1.idMatch1}) }}">  <button class="btn d-grid btn-danger text-white"> show  </button>  </a>
                    <a href="{{ path('app_match1_edit', {'id_match1': match1.idMatch1}) }}"> <button class="btn d-grid btn-danger text-white"> edit  </button> </a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="8">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_match1_new') }}"> <button class="btn d-grid btn-danger text-white"> Create new  </button> </a>
                        <a href="{{ path('generatePDF') }}"> <button class="btn d-grid btn-danger text-white"> GENERATE Excel  </button> </a>
                        <a href="{{ path('place') }}"> <button class="btn d-grid btn-danger text-white"> Envoyer un E-mail  </button> </a>

                    </div>
                </div>
            </div>
        </div>

        <!-- ============================================================== -->
        <!-- End PAge Content -->
        <!-- ============================================================== -->
        <!-- ============================================================== -->
        <!-- Right sidebar -->
        <!-- ============================================================== -->
        <!-- .right-sidebar -->
        <!-- ============================================================== -->
        <!-- End Right sidebar -->
        <!-- ============================================================== -->
    </div>
    <nav aria-label="Page navigation example">
        <ul class="pagination justify-content-center">
            {% for i in 1..nbrPage %}
            <li class="page-item"><a class="page-link" href="http://127.0.0.1:8000/match/?page= {{ i }} ">{{ i }} </a></li>
                    {% endfor %}
                    </ul>
                </nav>
    <!-- ============================================================== -->
    <!-- End Container fluid  -->
    <!-- ============================================================== -->
    <!-- ============================================================== -->
    <!-- footer -->
    <!-- ============================================================== -->
    <footer class="footer text-center">
    </footer>
    <!-- ============================================================== -->
    <!-- End footer -->
    <!-- ============================================================== -->
</div>
    <!-- ============================================================== -->
    <!-- End Page wrapper  -->
    <!-- ============================================================== -->
    </div>



    </body>
    </html>
{% endblock %}
