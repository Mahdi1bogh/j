{% extends 'base.html.twig' %}

{% block title %}Tournois index{% endblock %}

{% block tableau %}
<div class="page-wrapper">
    <!-- ============================================================== -->
    <!-- Bread crumb and right sidebar toggle -->
    <!-- ============================================================== -->
    <div class="page-breadcrumb bg-white">
        <div class="row align-items-center">
            <div class="col-lg-3 col-md-4 col-sm-4 col-xs-12">
                <h4 class="page-title">Users list</h4>
            </div>
            <div class="col-lg-9 col-sm-8 col-md-8 col-xs-12">
                <div class="d-md-flex">
                    <ol class="breadcrumb ms-auto">



                    </ol>

                </div>
            </div>
        </div>
        <!-- /.col-lg-12 -->
    </div>
    <!-- ============================================================== -->
    <!-- End Bread crumb and right sidebar toggle -->
    <!-- ============================================================== -->
    <!-- ============================================================== -->
    <!-- Container fluid  -->
    <!-- ============================================================== -->
    <div class="container-fluid">
        <!-- ============================================================== -->
        <!-- Start Page Content -->
        <!-- ============================================================== -->
        <div class="row">
            <div class="col-sm-12">
                <div class="white-box">
                    <h3 class="box-title">Users list</h3>

                    <div class="table-responsive">

                    <h1>Tournois </h1>

    <table class="table">
        <thead>
            <tr>

                <th>Nom_tournois</th>
                <th>Rank_tournois</th>
                <th>Date_tournois</th>
{#                <th>Id_match</th>#}
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for tournoi in tournois %}
            <tr>

                <td>{{ tournoi.nomTournois }}</td>
                <td>{{ tournoi.rankTournois }}</td>
                <td>{{ tournoi.dateTournois }}</td>
{#                <td>{{ tournoi.idMatch }}</td>#}
                <td>
                    <a href="{{ path('app_tournois_show', {'id': tournoi.idTournois}) }}"> <button class="btn d-grid btn-danger text-white"> show </button> </a>
                    <a href="{{ path('app_tournois_edit', {'id': tournoi.idTournois}) }}"> <button class="btn d-grid btn-danger text-white"> edit  </button></a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="7">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_tournois_new') }}">Create new</a>
{% endblock %}
