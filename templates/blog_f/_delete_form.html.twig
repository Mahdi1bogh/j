<form method="post" action="{{ path('app_blog_f_delete', {'id_blog': blog.id_blog}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ blog.id_blog) }}">
   <a >
    <button class="btn btn-outline-warning" target="_blank"  >Delete</button>
   </a>
</form>
