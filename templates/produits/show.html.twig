{% extends 'base.html.twig' %}

{% block title %}Produits{% endblock %}

{% block body %}
    <h1>Produits</h1>

    <table class="table">
       
            <tr>
                <th>Id_produit</th>
                <td>{{ produit.idProduit }}</td>
            </tr>
            <tr>
                <th>Nom_produit</th>
                <td>{{ produit.nomProduit }}</td>
            </tr>
            <tr>
                <th>Nb_pts</th>
                <td>{{ produit.nbPts }}</td>
            </tr>
            
        
    </table>

    <a href="{{ path('app_produits_index') }}">back to list</a>

    <a href="{{ path('app_produits_edit', {'id_produit': produit.id_produit}) }}">edit</a>

    {{ include('produits/_delete_form.html.twig') }}
{% endblock %}
